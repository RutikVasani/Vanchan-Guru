import 'package:flutter/material.dart';

class MCepage extends StatelessWidget {
  const MCepage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Ce Material"),
      ),
    );
  }
}
