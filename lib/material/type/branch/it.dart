import 'package:flutter/material.dart';

class MItpage extends StatelessWidget {
  const MItpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("It Material"),
      ),
    );
  }
}
