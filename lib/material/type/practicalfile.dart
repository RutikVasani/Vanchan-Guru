import 'package:flutter/material.dart';

class PracticalFilePage extends StatelessWidget {
  const PracticalFilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Practical File")),
    );
  }
}