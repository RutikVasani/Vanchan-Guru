import 'package:flutter/material.dart';

class QuestionPaperPage extends StatelessWidget {
  const QuestionPaperPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Question Paper")),
    );
  }
}
