import 'package:flutter/material.dart';

class MiniProjectPage extends StatelessWidget {
  const MiniProjectPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Mini Project Page")),
    );
  }
}